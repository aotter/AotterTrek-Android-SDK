<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.aotter.net.treksampleapp"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.NativeListViewActivity">
        </activity>
        <activity android:name=".activity.VideoListViewActivity">
        </activity>
        <activity android:name=".activity.InterActiveListViewActivity">
        </activity>
        <activity
            android:name="com.aotter.net.trek.ads.video.NativeVideoPlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"/>
    </application>

</manifest>